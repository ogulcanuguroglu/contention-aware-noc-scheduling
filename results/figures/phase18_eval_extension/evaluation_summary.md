# Phase 18B Evaluation Extension — Summary

Generated by `scripts/generate_phase18_eval_extension.py`.

---

## 1. Experiment Configuration

### Main grid

| Parameter | Value |
|-----------|-------|
| DAG families | chain (10 tasks), fork (9 tasks), out-tree (7 tasks), fork-join (14 tasks) |
| CCR values | 0.1, 1.0, 5.0, 10.0 |
| alpha | 0.0 |
| beta | 1.0 |
| NoC | 4×4 MeshNoC, XY routing |
| Seed | 0 (single seed, deterministic) |
| Schedulers | HEFT, CD-LS, Proposed CA-D |

### Alpha/hop-latency sensitivity

| Parameter | Value |
|-----------|-------|
| alpha values | 0.0, 1.0, 5.0 |
| CCR values | 1.0, 5.0 |
| DAG families | all four (same sizes) |

---

## 2. Metric Definitions

| Metric | Definition |
|--------|-----------|
| native_makespan | Scheduler's own makespan before replay |
| replayed_makespan | Makespan after `replay_under_contention` (fair NoC model) |
| native_speedup_vs_heft | HEFT native / scheduler native |
| replayed_speedup_vs_heft | HEFT replayed / scheduler replayed — **primary metric** |
| replay_overhead_ratio | replayed / native — shows optimism of native model |
| task_instance_ratio (TIR) | total scheduled instances / original task count; TIR=1.0 → no duplication |
| duplicate_count | total duplicate task instances |
| active_processor_count | processors with at least one task instance |

---

## 3. Communication Model and alpha=0 Justification

Communication duration formula:
```
duration = alpha × hop_count + beta × communication_volume
```

The main experiments set alpha=0 and beta=1 to isolate bandwidth-driven communication and link-level contention. In this setting, communication duration depends on data volume, while placement affects execution through route reservation conflicts rather than a fixed hop-distance penalty. This keeps CCR independent of topology and makes the duplication benefit easier to interpret. A non-zero alpha is evaluated separately as a hop-latency sensitivity check.

---

## 4. Key Observations

### 4.1 Chain — Negative Control

At all CCR values, CA-D and CD-LS replayed speedup = 1.000× (equal to HEFT). The chain DAG has no parallelism to exploit. Tasks must execute sequentially regardless of placement. Duplication can eliminate communication costs but cannot reduce the critical-path length. This is the expected negative/control result.

| CCR | HEFT | CD-LS | CA-D |
|-----|------|-------|------|
| 0.1 | 1.000× | 1.000× | 1.000× |
| 1.0 | 1.000× | 1.000× | 1.000× |
| 5.0 | 1.000× | 1.000× | 1.000× |
| 10.0 | 1.000× | 1.000× | 1.000× |

### 4.2 Fork — Duplication Benefit, CD-LS ≡ CA-D

On a fork DAG (one root, 8 independent leaves), CD-LS and Proposed CA-D produce identical schedules at every CCR value. Both duplicate the root T0 locally near each leaf, eliminating all outgoing communication. This is expected: the fork has only one ancestor level, so parent-only duplication (CD-LS) and recursive ancestor duplication (Proposed CA-D) reduce to the same decision.
Maximum replayed speedup (CCR=10.0): 3.258×.

| CCR | HEFT | CD-LS | CA-D | TIR (CA-D) |
|-----|------|-------|------|-----------|
| 0.1 | 1.000× | 1.157× | 1.157× | 1.78 |
| 1.0 | 1.000× | 2.811× | 2.811× | 1.78 |
| 5.0 | 1.000× | 2.778× | 2.778× | 1.78 |
| 10.0 | 1.000× | 3.258× | 3.258× | 1.78 |

### 4.3 Out-tree — CA-D Better than CD-LS

On the out-tree (depth=2, branching_factor=2), CA-D consistently outperforms CD-LS. At CCR=1.0: CA-D 2.262×, CD-LS 1.631×. CD-LS overhead at CCR=1.0 is 1.219× (native model was optimistic). CA-D's recursive ancestor duplication reaches deeper into the DAG, eliminating more communication links and achieving a lower, contention-safe makespan.

| CCR | HEFT | CD-LS (replayed) | CA-D (replayed) | CD-LS overhead |
|-----|------|------------------|-----------------|----------------|
| 0.1 | 1.000× | 1.000× | 1.000× | 1.000× |
| 1.0 | 1.000× | 1.631× | 2.262× | 1.219× |
| 5.0 | 1.000× | 1.595× | 2.070× | 1.000× |
| 10.0 | 1.000× | 1.595× | 2.070× | 1.000× |

### 4.4 Fork-join — Strongest CA-D Advantage; CD-LS Degrades Under Replay

The fork-join DAG (4 branches, length 3) produces the most compelling results. Proposed CA-D consistently achieves replay_overhead_ratio = 1.0× (its native model accurately predicts contention). CD-LS degrades significantly under fair replay: at CCR=10.0, CD-LS replayed speedup = 0.684× — **below HEFT baseline** — with overhead 1.383×. The contention-blind native model substantially underestimated the actual makespan.
CA-D at CCR=5.0: 1.342× replayed speedup, overhead=1.0×.

| CCR | HEFT | CD-LS (replayed) | CA-D (replayed) | CD-LS overhead | CA-D overhead |
|-----|------|------------------|-----------------|----------------|---------------|
| 0.1 | 1.000× | 1.033× | 1.033× | 1.000× | 1.000× |
| 1.0 | 1.000× | 1.042× | 1.203× | 1.155× | 1.000× |
| 5.0 | 1.000× | 1.020× | 1.342× | 1.246× | 1.000× |
| 10.0 | 1.000× | 0.684× ⚠ | 1.257× | 1.383× | 1.000× |

⚠ CD-LS replayed speedup below 1.0 indicates performance **worse than HEFT** after fair replay.

### 4.5 Alpha/Hop-Latency Sensitivity

Chain: unchanged at all alpha values (sequential structure, no parallelism).

Fork: nearly unchanged at CCR=5.0 (HEFT serializes; duplication eliminates all communication regardless of hop cost). Small improvement at CCR=1.0 with higher alpha (hop penalty makes remote HEFT placement more expensive).

Fork-join at CCR=5.0: CA-D replayed speedup = 1.342× (alpha=0.0), 1.694× (alpha=1.0), 1.299× (alpha=5.0). The qualitative conclusion — CA-D outperforms CD-LS — holds across all alpha values.

Out-tree at CCR=1.0: lower CA-D benefit with higher alpha, because added hop penalty discourages some cross-processor placements that CA-D relies on. At CCR=5.0 the result is unchanged.

**Conclusion:** Alpha sensitivity does not reverse the qualitative findings. CA-D's contention-aware advantage is robust to the addition of a fixed hop penalty.

---

## 5. Recommended Figures for Report

| Figure | Role | File |
|--------|------|------|
| figA_ccr_speedup | Main text — CCR sweep replayed speedup | figA_ccr_speedup.pdf |
| figB_replay_overhead | Main text — replay overhead | figB_replay_overhead.pdf |
| figD_fork_join_ccr5 | Main text — fork-join Gantt (CA-D vs CD-LS) | figD_fork_join_ccr5.pdf |
| figE_alpha_sensitivity | Main text — hop sensitivity | figE_alpha_sensitivity.pdf |
| figC_tir | Supplement — duplication cost | figC_tir.pdf |
| figD_fork_ccr5 | Supplement — fork Gantt | figD_fork_ccr5.pdf |
| figD_out_tree_ccr5 | Supplement — out-tree Gantt | figD_out_tree_ccr5.pdf |

---

## 6. Expected vs Observed Patterns

| Pattern | Expected | Observed |
|---------|----------|----------|
| Chain: no benefit from duplication | Yes | ✓ (1.000× all CCR) |
| Fork: CD-LS ≡ CA-D | Yes (one ancestor level) | ✓ (identical at all CCR) |
| Out-tree: CA-D > CD-LS | Yes (recursive multi-level dup) | ✓ |
| Fork-join: CA-D > CD-LS under replay | Yes | ✓ (CD-LS < 1.0× at CCR=10) |
| CD-LS optimistic at high CCR | Yes (contention-blind) | ✓ (overhead up to 1.38×) |
| CA-D overhead ≈ 1.0× | Yes (contention-aware) | ✓ (always 1.0×) |
| Alpha sensitivity preserves conclusions | Expected | ✓ (qualitative ranking unchanged) |
